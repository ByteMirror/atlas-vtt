# Avatar Images

For the Obsidian plugin, the avatar images will be served from the Atlas VTT website.
The getUserAvatarUrl function in avatarUtils.ts handles constructing the full URL.

No need to include avatar images in the plugin bundle.